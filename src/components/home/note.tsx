import { motion } from "framer-motion";
import React from "react";



/**
 * NotebookCoverCard
 * A small clickable notebook cover card. Perfect for opening your daily diary modal.
 *
 * Usage:
 * <NotebookCoverCard onOpen={() => setOpen(true)} />
 */
export default function NotebookCoverCard() {
  return (
    <div className="w-64 h-90 bg-green-100 flex justify-center items-center cursor-pointer">
        <p>дар</p>
    </div>
  );
}
